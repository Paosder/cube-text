import { Meta, Story, ArgsTable, Canvas } from "@storybook/addon-docs/blocks";
import { CubeText, CubeTextProps } from "./CubeText";

<Meta title="CubeText/Introduction" component={CubeText} />

export const Template = (args) => <CubeText {...args} />;

# CubeText

# Example

<Canvas>
  <Story name="CubeText" max-height="400px" args={{ text: "Cube." }}>
    {(args) => <CubeText {...args} style={{ height: "400px" }} />}
  </Story>
</Canvas>

## ArgsTable

<ArgsTable story="CubeText" />
